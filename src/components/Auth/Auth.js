import React from 'react';
import PropTypes from 'prop-types';
import { AuthWrapper } from './Auth.styled';

const Auth = () => (
 <AuthWrapper>
    Auth Component
 </AuthWrapper>
);

Auth.propTypes = {};

Auth.defaultProps = {};

export default Auth;
